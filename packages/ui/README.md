# @v-monorepo/ui

共享 UI：shadcn (base-sera) + Base UI。应用入口引入一次全局样式。

```ts
import "@v-monorepo/ui/globals.css";
import { Button } from "@v-monorepo/ui/components/button";
import { Toaster, toast } from "@v-monorepo/ui/components/toast";
```
