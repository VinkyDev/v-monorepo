{
  "name": "server",
  "type": "module",
  "version": "0.0.0",
  "scripts": {
    "build": "tsup",
    "dev": "tsx watch src/index.ts",
    "start": "pnpm run build && NODE_ENV=production node dist/index.cjs",
    "lint": "eslint . --ext .ts,.tsx --fix",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.0",
    "hono": "catalog:",
    "logger": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsup": "^8.5.0",
    "tsx": "^4.20.6",
    "typescript": "catalog:"
  }
}
